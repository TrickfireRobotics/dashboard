/** @type {import("prettier").Config} */
const config = {
    plugins: ["prettier-plugin-tailwindcss"],
    semi: true,
    singleQuote: false,
    tabWidth: 4,
    trailingComma: "es5",
    printWidth: 100,
    endOfLine: "lf",
};

export default config;
