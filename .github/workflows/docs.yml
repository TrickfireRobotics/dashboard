name: Sync Docs

on:
    push:
        branches: [main]
        paths:
            - "docs/**"
            - "docs.config.ts"
    workflow_dispatch:

jobs:
    sync:
        uses: TrickfireRobotics/docs/.github/workflows/sync-docs.yml@main
        secrets: inherit
