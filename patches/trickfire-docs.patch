diff --git a/dist-cli/cli.js b/dist-cli/cli.js
index e74aa8d208dcb01099a09084e26ac72e16f4cf0d..7940e22d04ba19e7ec9ffef446d11e25ff268f3e 100755
--- a/dist-cli/cli.js
+++ b/dist-cli/cli.js
@@ -64,6 +64,14 @@ export default defineConfig({
                 ignored: ["!**/node_modules/trickfire-docs/**"],
             },
         },
+        css: {
+            // Vite's PostCSS config auto-discovery walks up the filesystem tree
+            // unbounded and will pick up an unrelated postcss.config.* from the
+            // host repository this framework is installed into (e.g. a Next.js
+            // + Tailwind app), even though this site's own styles never need it.
+            // An explicit (empty) config here skips that file search entirely.
+            postcss: { plugins: [] },
+        },
     },
     integrations: [
         starlight({
